import buble from 'rollup-plugin-buble'
import json from 'rollup-plugin-json'
import uglify from 'rollup-plugin-uglify'
import { condensed as banner } from './rollup.conf.banner'

export default {
	banner,
	input: 'src/index.js',
	plugins: [
		json(),
		buble(),
		uglify({
			output: {
				comments: /@license miniraf/
			}
		})
	],
	output: {
		format: 'iife',
		file: 'dist/miniraf.min.js',
		name: 'miniraf'
	}
}
